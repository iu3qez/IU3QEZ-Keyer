/**
 * @file wifi_secrets.h.example
 * @brief Example WiFi credentials for development
 *
 * USAGE:
 * 1. Copy this file to wifi_secrets.h (same directory)
 * 2. Edit wifi_secrets.h with your WiFi credentials
 * 3. wifi_secrets.h is gitignored and will NOT be committed
 *
 * NOTE: This is only for development/testing convenience.
 * Production devices are configured via Web UI (http://192.168.4.1/)
 */

#pragma once

// Uncomment and fill in your development WiFi credentials
// #define WIFI_DEV_SSID "YourWiFiNetwork"
// #define WIFI_DEV_PASSWORD "YourPassword"

// AP mode fallback (for first-time setup)
#define WIFI_AP_SSID "Keyer-AP"
#define WIFI_AP_PASSWORD ""  // Empty = open network
