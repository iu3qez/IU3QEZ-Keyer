/**
 * @file console_commands.cpp
 * @brief Built-in command handlers for serial console
 *
 * Implements standard console commands:
 * - help: Display available commands and usage information
 * - set: Modify parameter values (integrates with YAML parameter registry)
 * - show: Display parameter values or system status
 * - debug: Control ESP-IDF logging levels (transient, not persisted)
 * - save: Persist current configuration to NVS
 * - reboot: System restart (requires "confirm" argument)
 * - factory-reset: Erase NVS and reboot (requires "confirm" argument)
 */

namespace ui {

// Command handlers will be implemented in subsequent tasks

} // namespace ui
